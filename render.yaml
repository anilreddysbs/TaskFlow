databases:
  - name: taskflow-db
    databaseName: taskflow
    user: taskflow_user
    plan: free

services:
  # Redis Cache & Celery Broker
  - type: redis
    name: taskflow-redis
    plan: free
    ipAllowList: [] # Only allow internal network traffic

  # Web Service for Django App
  - type: web
    name: taskflow-web
    runtime: docker
    dockerfilePath: Dockerfile
    plan: free
    preDeployCommand: "python manage.py migrate"
    envVars:
      - key: DEBUG
        value: "False"
      - key: DJANGO_SETTINGS_MODULE
        value: "taskflow.settings"
      - key: DJANGO_SECRET_KEY
        generateValue: true
      - key: DATABASE_URL
        fromDatabase:
          name: taskflow-db
          property: connectionString
      - key: REDIS_URL
        fromService:
          type: redis
          name: taskflow-redis
          property: connectionString
      - key: CELERY_BROKER_URL
        fromService:
          type: redis
          name: taskflow-redis
          property: connectionString
      - key: CELERY_RESULT_BACKEND
        fromService:
          type: redis
          name: taskflow-redis
          property: connectionString
      - key: DB_SSL_REQUIRE
        value: "True"
      - key: ALLOWED_HOSTS
        value: "*"
      - key: CORS_ALLOW_ALL_ORIGINS
        value: "True"

  # Celery Background Worker
  - type: worker
    name: taskflow-worker
    runtime: docker
    dockerfilePath: Dockerfile
    dockerCommand: "celery -A taskflow worker --loglevel=info --concurrency=4"
    plan: starter
    envVars:
      - key: DEBUG
        value: "False"
      - key: DJANGO_SETTINGS_MODULE
        value: "taskflow.settings"
      - key: DJANGO_SECRET_KEY
        generateValue: true
      - key: DATABASE_URL
        fromDatabase:
          name: taskflow-db
          property: connectionString
      - key: REDIS_URL
        fromService:
          type: redis
          name: taskflow-redis
          property: connectionString
      - key: CELERY_BROKER_URL
        fromService:
          type: redis
          name: taskflow-redis
          property: connectionString
      - key: CELERY_RESULT_BACKEND
        fromService:
          type: redis
          name: taskflow-redis
          property: connectionString
      - key: DB_SSL_REQUIRE
        value: "True"

  # Celery Beat Scheduler
  - type: worker
    name: taskflow-beat
    runtime: docker
    dockerfilePath: Dockerfile
    dockerCommand: "celery -A taskflow beat --loglevel=info --scheduler django_celery_beat.schedulers:DatabaseScheduler"
    plan: starter
    envVars:
      - key: DEBUG
        value: "False"
      - key: DJANGO_SETTINGS_MODULE
        value: "taskflow.settings"
      - key: DJANGO_SECRET_KEY
        generateValue: true
      - key: DATABASE_URL
        fromDatabase:
          name: taskflow-db
          property: connectionString
      - key: REDIS_URL
        fromService:
          type: redis
          name: taskflow-redis
          property: connectionString
      - key: CELERY_BROKER_URL
        fromService:
          type: redis
          name: taskflow-redis
          property: connectionString
      - key: CELERY_RESULT_BACKEND
        fromService:
          type: redis
          name: taskflow-redis
          property: connectionString
      - key: DB_SSL_REQUIRE
        value: "True"
